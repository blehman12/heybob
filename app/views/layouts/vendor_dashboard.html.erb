<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#0a0a0a">
    <title><%= content_for?(:title) ? "#{yield(:title)} — Vendor" : "Vendor Dashboard" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Syne:wght@700;800&family=DM+Sans:wght@400;500;600&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "visitor", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "vendor_dashboard", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= yield :head %>
  </head>

  <body class="vendor-body">

    <header class="vd-topbar">
      <div class="vd-topbar__left">
        <% if content_for?(:back_link) %>
          <%= yield :back_link %>
        <% else %>
          <%= link_to vendor_root_path, class: 'vd-back' do %>
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
          <% end %>
        <% end %>
        <span class="vd-topbar__title"><%= content_for?(:title) ? yield(:title) : 'Vendor' %></span>
      </div>
      <div class="vd-topbar__right">
        <span class="vd-user"><%= current_user.first_name %></span>
        <%= button_to destroy_user_session_path, method: :delete,
                      class: 'vd-signout',
                      form: { style: 'display:inline' } do %>
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
        <% end %>
      </div>
    </header>

    <main class="vd-main">
      <% flash.each do |type, message| %>
        <div class="vd-flash vd-flash--<%= type == 'notice' ? 'success' : 'error' %>">
          <%= message %>
        </div>
      <% end %>

      <%= yield %>
    </main>

  </body>
</html>
