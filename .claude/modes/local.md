## Mode: LOCAL (no deploy)

After completing all changes and tests:
- DO NOT git add, commit, or push anything
- Report: test results only (pass/fail + any failures)
- If tests fail: show the failure output so it can be reviewed
